interface:
  display_name: "Debug Tests"
  short_description: "Use this agent for closed-loop test debugging"
  default_prompt: "Use $axiom-debug-tests to debug tests in this project."
policy:
  allow_implicit_invocation: false
